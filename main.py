import sys
import argparse
import youtuber_fetcher as  yt
import os


def main():
    # url = os.getenv("URL")
    # youtube_id = yt.get_id(url)
    # mp3_file_nm = yt.save_audio(url)
        
    # title_suggestion = (
    #     mp3_file_nm
    #     .lower()
    #     .replace(" ","_")
    #     .replace("./", "")
    #     .split(".")[0]
    # )
    # transcription_file_nm = yt.get_transcript(youtube_id, title_suggestion, "de")
    # yt.slice_mp3_from_json(mp3_file_nm, transcription_file_nm)




if __name__ == '__main__':
    main()